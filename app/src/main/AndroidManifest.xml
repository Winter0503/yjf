<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cn.ygyg.yjf">

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".modular.activity.RegisterActivity" />
        <activity android:name=".modular.internet.activity.NewAccountActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".modular.activity.UserAgreementActivity" />
        <activity android:name=".modular.activity.LoginActivity" />
        <!-- <activity android:name=".modular.activity.RegisterActivity" /> -->
        <!-- <activity android:name=".modular.activity.RegisterActivity" /> -->
        <activity android:name=".modular.internet.activity.NewAccountSuccessActivity" />
        <activity android:name=".modular.internet.activity.ListSelectActivity"></activity>
    </application>

</manifest>